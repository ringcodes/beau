spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/beau?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
oss:
  tencent:
    enable: true # 是否启用
    bucket: #bucket
    secret-key: #secret key
    access-key: #access key
    region-name: #region name
